<template>
  <div class="p-6">
    <h1 class="text-2xl font-bold">📢 News Detail</h1>
    <p class="mt-4">คุณกำลังดูข่าวหมายเลข: <b>{{ id }}</b></p>

    <!-- ตัวอย่างดึงข้อมูลจาก array -->
    <div v-if="newsItem" class="mt-6 p-4 border rounded">
      <h2 class="text-xl font-semibold">{{ newsItem.title }}</h2>
      <p class="mt-2">{{ newsItem.content }}</p>
    </div>

    <div v-else class="mt-6 text-red-600">
      ❌ ไม่พบข่าวที่คุณต้องการ
    </div>
  </div>
</template>

<script setup>
import { useRoute } from 'vue-router'

const route = useRoute()
const id = route.params.id   // ค่า id จาก URL เช่น 1, 2, 3

// ตัวอย่างข้อมูลข่าว mock data
const newsData = [
  { id: 1, title: "เปิดตัวเว็บใหม่!", content: "เว็บไซต์ใหม่พร้อมฟีเจอร์ข่าวสารและกิจกรรม." },
  { id: 2, title: "ประกาศหยุดเรียน", content: "โรงเรียนจะหยุดทำการวันที่ 20 สิงหาคม 2025." },
  { id: 3, title: "การแข่งขันกีฬาสี", content: "กีฬาสีประจำปีเริ่มต้นวันที่ 25 สิงหาคมนี้." },
]

// หา item ที่ตรงกับ id ที่ส่งมา
const newsItem = newsData.find(item => item.id.toString() === id)
</script>
